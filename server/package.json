{
    "name": "nalburdeposu-server",
    "version": "1.0.0",
    "description": "Nalbur Deposu Backend - API & Admin Panel Monorepo",
    "author": "Nalbur Deposu",
    "private": true,
    "scripts": {
        "dev": "concurrently -n \"API,ADMIN\" -c \"blue,magenta\" \"npm run dev --prefix api\" \"npm run dev --prefix admin\"",
        "install:all": "npm install && npm install --prefix api && npm install --prefix admin"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}
